version: 2.1

jobs:
  build:
    machine: true
    resource_class: tar3kom/testing_lab
    steps:
      - run: echo "Hello From Circle CI Runner"
      - run: docker --version
      - run: whoami
      - run: docker ps

workflows:
  build_and_test:
    jobs:
      - build
